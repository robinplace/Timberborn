the key thing

status quo
tick is a gate
any tick-read property sync before tick
other properties sync whenever

to allow out-of-order sync
tick is not a gate
any tick-set property sync whenever
other properties sync whenever
to minimize throughput tick must still be mostly deterministic
nah tick-set properties are enormous

